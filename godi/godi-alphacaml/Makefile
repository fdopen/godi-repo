# This file was automatically generated by godiva version 0.9.5.1
.include "../../mk/bsd.prefs.mk"

VERSION=        20061214
PKGNAME=        godi-alphacaml-${VERSION}
PKGREVISION=    0
DISTNAME=       alphaCaml-20061214
DISTFILES=      alphaCaml-20061214.tar.gz
CATEGORIES=     godi
MASTER_SITES=   http://cristal.inria.fr/~fpottier/alphaCaml/
MAINTAINER=	wodi@ml.ignorelist.com
HOMEPAGE=       http://cristal.inria.fr/~fpottier/alphaCaml/
COMMENT=        A tool that helps deal with alpha-conversion issues.

# confopt defaults:


AUTOGENERATE_PLIST = yes
PKG  =          godi-alphacaml
MAKE_FLAGS=     PREFIX=${PREFIX} LIBDIR=${PREFIX}/lib BINDIR=${PREFIX}/bin MANDIR=${PREFIX}/man 
DEPENDS+= godi-ocaml>=3.08:../../godi/godi-ocaml
BUILD_DEPENDS+= godi-findlib-[0-9]*:../../godi/godi-findlib

# HAS_CONFIGURE = yes

USE_GMAKE = yes

MAKE_ENV+=  ${BUILD_OCAMLFIND_ENV} PKGBASE=${PKGBASE:Q}

pre-configure-copy:
.	if exists(files)
	    cd files && ${PAX} -rw -pp . ${WRKSRC}
.	endif

pre-configure: pre-configure-copy

ALL_TARGET=     all
.if ${GODI_HAVE_OCAMLOPT} == "yes"
# ALL_TARGET+= opt
.endif

post-install:
	mkdir -p ${PREFIX}/doc/${PKG}
.	for DOC in alphaCaml.pdf
	    install -m 0644 ${WRKSRC}/${DOC} ${PREFIX}/doc/${PKG}
.	endfor

.include "../../mk/bsd.pkg.mk"
