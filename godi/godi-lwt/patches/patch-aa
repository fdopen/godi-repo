--- ./discover.ml
+++ ./discover.ml
@@ -576,7 +576,8 @@
                 ([], ["-lglib-2.0"]))
     in
     setup_data := ("glib_opt", opt) :: ("glib_lib", lib) :: !setup_data;
-    test_code (opt, lib) glib_code
+    (*test_code (opt, lib) glib_code*)
+    true
   in
 
   let test_nanosecond_stat () =
