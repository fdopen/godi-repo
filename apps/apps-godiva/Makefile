# This file was automatically generated by godiva version 0.9.5.1
.include "../../mk/bsd.prefs.mk"

VERSION=        0.9.7
PKGNAME=        apps-godiva-${VERSION}
PKGREVISION=    0
DISTNAME=       godiva-0.9.7
DISTFILES=      godiva-0.9.7.tar.gz
CATEGORIES=     apps
MASTER_SITES=   http://projects.phauna.org/godiva/release/
MAINTAINER=	wodi@ml.ignorelist.com
HOMEPAGE=       http://projects.phauna.org/godiva/
COMMENT=        High-level tool for simplifying the creation of GODI packages.

# confopt defaults:


AUTOGENERATE_PLIST = yes
PKG  =          apps-godiva
MAKE_FLAGS=     PREFIX=${PREFIX} LIBDIR=${PREFIX}/lib BINDIR=${PREFIX}/bin MANDIR=${PREFIX}/man 
DEPENDS+=	godi-ocaml>=3.07:../../godi/godi-ocaml
BUILD_DEPENDS+= godi-ocamlmakefile-[0-9]*:../../godi/godi-ocamlmakefile
BUILD_DEPENDS+= godi-xstrp4>=1.4:../../godi/godi-xstrp4

# HAS_CONFIGURE = yes

USE_GMAKE = yes

MAKE_ENV+=  ${BUILD_OCAMLFIND_ENV} PKGBASE=${PKGBASE:Q}

pre-configure-copy:
.	if exists(files)
	    cd files && ${PAX} -rw -pp . ${WRKSRC}
.	endif

pre-configure: pre-configure-copy

ALL_TARGET=     all
.if ${GODI_HAVE_OCAMLOPT} == "yes"
# ALL_TARGET+= opt
.endif

post-install:
	mkdir -p ${PREFIX}/doc/${PKG}
.	for DOC in COPYING
	    install -m 0644 ${WRKSRC}/${DOC} ${PREFIX}/doc/${PKG}
.	endfor

.include "../../mk/bsd.pkg.mk"
